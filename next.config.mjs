/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: "xiazeesfgmltejff.public.blob.vercel-storage.com"
            }
        ]
    }
};

export default nextConfig;
